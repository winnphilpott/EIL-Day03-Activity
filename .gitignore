# R session artifacts
.Rhistory
.RData
.Rproj.user/
*.Rproj

# renv
renv/library/
renv/staging/

# Output files (regenerated by scripts — commit selectively if desired)
# output/figures/ is tracked so figures are visible on GitHub
# output/tables/ is tracked so tables are visible on GitHub
output/tables/*
output/statistics/*
!output/tables/.gitkeep
!output/tables/*.csv
!output/statistics/.gitkeep

# Large / sensitive raw data (add exceptions below as needed)
# data/raw/*
# !data/raw/.gitkeep

# macOS
.DS_Store

# Python / Jupyter (if any exploratory notebooks)
__pycache__/
*.py[cod]
.ipynb_checkpoints/

# Credentials and secrets
.env
*.pem
*.key
secrets/
